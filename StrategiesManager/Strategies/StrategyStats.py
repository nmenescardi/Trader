
#TODO: Create StrategyStats class. To handle saved data related to strategy/ticker. Eg:
	# StrategyStats.save_order(ticker, strategy_name, locals()?? )
	# StrategyStats.get_daily_sma(ticker, period=100)
	# StrategyStats.get_daily_atr(ticker, period=14)
	# StrategyStats.is_there_a_recent_order(ticker, strategy_name, days_between_orders)